# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**Accounts Receivable Agent** — A Python-based project for automating or assisting with accounts receivable operations.

The project is in early stages with foundational structure still being established. This file will be updated as the architecture solidifies.

## Getting Started

This is a Python project. When the project is ready for development:

### Typical Development Commands (to be confirmed once structure is established)

- **Run the main application:** `python main.py`
- **Install dependencies:** `pip install -r requirements.txt` (when requirements.txt is created)
- **Run tests:** `pytest` (when test suite is set up)
- **Lint/format code:** `black .` and `flake8 .` (when linters are configured)

## Architecture Notes

**Current Status:** Project skeleton only.

**Expected Structure (to be developed):**
- `main.py` — Entry point for the agent application
- `Workflow.md` — Documentation for the agent's workflow/processes
- Supporting modules for AR operations (TBD)

## Next Steps for Setup

1. **Define requirements** — Document the key responsibilities and features of the Accounts Receivable Agent
2. **Create requirements.txt** — Pin Python dependencies
3. **Establish project structure** — Create subdirectories for:
   - Core agent logic
   - Data models
   - Integrations (if needed)
   - Tests
4. **Populate Workflow.md** — Document the AR workflows this agent will handle
5. **Add configuration** — Set up any config files needed (environment variables, settings, etc.)

## Development Patterns (to be established)

- Error handling and logging strategy
- Data validation approach
- Integration patterns with external systems (accounting software, databases, etc.)
- Testing strategy

---

**Note:** This file should be updated as the project evolves. Current guidance is provisional and awaiting project requirements clarification.
